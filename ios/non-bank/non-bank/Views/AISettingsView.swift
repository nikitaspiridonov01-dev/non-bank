import Foundation

// The previous AI-receipt-scanning settings screen was removed for v1.
// Cloud AI is now on by default and the backend URL is hard-coded —
// see `AISettings.swift`. The file is intentionally empty so the Xcode
// project reference stays valid without dragging dead UI code into the
// build; remove it from the project at the next chance.
